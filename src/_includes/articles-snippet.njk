
    <div class="blog-container">
        <div class="blog-box">
            <div class="blog-img">
               <image src="{{post.data.image}}" alt="{{post.data.imageAlt}}" class="snippet_image"/> 
            </div>
            <div class="blog-text">
                <span>{{post.data.author}}</span> on <span>{{post.date | postDate}}</span> 
                 <h3 class="blog-title"><a href="{{post.url}}">{{ post.data.title}}</a></h3>
                 <p class="snippet_body">{{post.data.description}}</p> 
                 <a href="{{post.url}}">Continue Reading</a>
            </div>
    </div>